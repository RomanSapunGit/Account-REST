In case of running that program you should add roles in table "roles"(using request "/create-authorities"), and also add environment variables (DB_PASSWORD, DB_USERNAME, MAIL_USERNAME, MAIL_PASSWORD)
